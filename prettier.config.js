/** @type {import('prettier').Config & import('prettier-plugin-tailwindcss').PluginOptions} */
export default {
  plugins: ["prettier-plugin-tailwindcss"],
  // Длина строки в символах (в терминах Prettier это не «px»)
  printWidth: 128,
};
